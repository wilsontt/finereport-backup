import Request from "./Request";
export default abstract class Response<T extends {}> {
    header: T;
    body: any;
    request?: Request<T>;
    constructor(header?: T, body?: any);
    abstract serialize(): Buffer;
}
